import React from "react";
import Card, { Image } from "../../components/ui/Card/Card";

const Home = () => {
  return (
    <div
      style={{
        display: "flex",
        width: "100%",
        height: "100vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
    </div>
  );
};

export default Home;
